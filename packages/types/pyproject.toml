[project]
name = "dr-types"
version = "0.1.0"
description = "Shared Pydantic models for Deep Research Agent"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dr_types"]
